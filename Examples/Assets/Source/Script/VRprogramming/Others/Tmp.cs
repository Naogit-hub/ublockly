using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tmp : MonoBehaviour
{
    public ProgrammableObject p;
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(p.name);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
